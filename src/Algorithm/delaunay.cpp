#include "delaunay.hpp"

#include "../Core/core.hpp"

void
DelaunayOne (Point * p, Mesh * output)
{
    VOID_USE (p);
    VOID_USE (output);

    return;
}

void
DelaunayTriangulation (Mesh * input, Mesh * output)
{
    VOID_USE (input);
    VOID_USE (output);

    return;
}